#if !defined(WIFI_CONFIGURATION_H)

#define WIFI_CONFIGURATION

const char *wifi_ssid = "${wifi_ssid}";
const char *wifi_password = "${wifi_password}";

#endif   /* !defined(WIFI_CONFIGURATION_H) */

